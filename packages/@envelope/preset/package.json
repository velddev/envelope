{
  "name": "@envelope/preset",
  "version": "0.0.1",
  "description": "Preset for Envelope using PandaCSS",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepare": "panda codegen --clean",
    "build": "tsup",
    "storybook": "start-storybook -p 6006",
    "build-storybook": "build-storybook"
  },
  "devDependencies": {
    "@pandacss/dev": "^0.9.0",
    "tsup": "^7.1.0",
    "typescript": "^5.1.3"
  }
}
